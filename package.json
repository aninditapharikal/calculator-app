{
    "name": "calculator-app",
    "version": "1.0.0",
    "main": "server.js",
    "license": "MIT",
    "dependencies": {
        "body-parser": "^1.19.0",
        "cors": "^2.8.5",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "firebase": "^8.2.9",
        "socket.io": "^3.1.1"
    },
    "scripts": {
        "start": "node server.js",
        "heroku-postbuild": "cd client && npm install && npm install && npm run build"
    }
}
